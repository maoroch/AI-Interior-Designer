"""
Мультиагентная система.

Контракт: каждый агент — async-функция вида

    async def run(scene: Scene, project: Project) -> Scene

которая читает нужные ей поля Scene, дополняет свои и возвращает новый Scene.
Агенты не имеют побочных эффектов на БД напрямую — сохранение делает
Scene Generator / orchestrator в app/tasks/pipeline_tasks.py. Это упрощает
тестирование каждого агента изолированно (chat-агент — исключение,
он читает/пишет напрямую через точечные патчи, см. agents/conversation).
"""
